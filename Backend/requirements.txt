# Core dependencies
python-dotenv>=1.0.0
openai>=1.12.0
requests>=2.31.0

# File operations and system access
watchdog>=3.0.0

# Data processing and visualization
pandas>=2.1.0
numpy>=1.24.3
matplotlib>=3.7.2
plotly>=5.17.0
seaborn>=0.12.2

# AI/ML for custom model training
# Note: don't lock TensorFlow/Torch too tightly — they conflict.
tensorflow>=2.13.0; sys_platform != "darwin"  # (macOS often needs special install)
torch>=2.0.0
scikit-learn>=1.3.0
transformers>=4.35.0

# GUI frameworks (pick one manually later if needed)
# streamlit>=1.28.0
# PyQt6>=6.5.2

# CLI + utilities
rich>=13.6.0
typer>=0.9.0
python-dateutil>=2.8.2
Pillow>=10.0.1
